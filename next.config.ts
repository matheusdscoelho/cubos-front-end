import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['cubos-challenge.s3.us-east-2.amazonaws.com', 'placehold.co'],
  },
};

export default nextConfig;
